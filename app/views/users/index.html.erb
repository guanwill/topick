<% page_header "Users" %>

<%= will_paginate %>


  <% @users.each do |user| %>

      <% if current_user != user %>
        <%= user.name %>
        <%= link_to 'Send message', new_message_path(to: user.id), class: 'btn btn-default btn-sm' %>
      <% end %>

  <% end %>


<%= will_paginate %>
